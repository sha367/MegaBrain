# Whether to enable the electron wrap, 1 is enable, 0 is disable
# Do not modify this one, it is managed by the package.json scripts
VITE_ELECTRON_WRAP=1

# Whether to enable launch llm, 1 is enable, 0 is disable
# Set as 0 if you are focused on renderer process only
RUN_LLM=1
